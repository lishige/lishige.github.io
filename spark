<h2>Spark概述</h2>
<blockquote><p>Spark本身就是一个输入量比较简单的大数据项目，一般使用Java或Python等常用的编程语言，使用很方便，可以激发我们了解更多与<strong>Spark</strong>、<strong>Python</strong>、<strong>Java</strong>等与大数据相关的技术、编程语言。</p>
</blockquote>
<p>学习基础的Spark语言的编程思想，和具体的应用相结合，帮助我们可以更好地统计出需要的信息；输出的时候很容易出现不需要的内容，还需要输入相关代码，重新整理数据，比较麻烦，以后输入信息的时候尽量使用简单的代码，同时要仔细阅读与车流量分析相关的内容，做好备注。</p>
<p><strong>MapReduce</strong>和<strong>Spark</strong>都是批处理框架。</p>
<h3>一、基本概念概述</h3>
<ol>
<li><strong>RDD</strong>：弹性分布式数据集的简称，是分布式内存的一个抽象概念，提供了一种高度受限的共享内存模型。</li>
<li><strong>DAG</strong>：有向无环图的简称，反应了RDD之间的依赖关系。</li>
<li><strong>Executor</strong>：运行在工作节点上的一个进程，负责运行任务，并为应用程序存储数据。</li>
<li><strong>应用</strong>：这就是用户编写的Spark应用程序。</li>
<li><strong>任务</strong>：运行在Executor上的工作单元。</li>
<li><strong>作业</strong>：一个作业包含多个RDD以及作用于相应RDD上的各种操作。</li>
<li><strong>阶段</strong>：是作业的基本调度单位，一个作业会分为多组任务。</li>

</ol>
<h3>RDD</h3>
<p>一个RDD就是一个分布式对象集合，本质上是一个只读的分区记录集合，每个RDD可以分成多个分区，每个分区就是一个<strong>数据集片段</strong>。并且一个RDD的不同分区可以被保存到集群中不同的节点上，从而可以在集群中的不同节点上进行并行计算。</p>
<h4>可以分为<strong>行动</strong>和<strong>转换</strong>两种类型。</h4>
<p><strong>行动</strong>：用于执行计算并制定输出的形式。</p>
<p><strong>转换</strong>：指定RDD之间的相互依赖关系。</p>
<p>行动和转换的<strong>主要区别：</strong>转换操作接受RDD并返回RDD，而行动操作接受RDD但是返回非RDD。</p>
<h3>二、基本特点概述</h3>
<h2>Spark运行架构特点</h2>
<ol>
<li>每个应用都有自己专属的<strong>Executor</strong>进程，并且该进程在应用运行期间一直驻留。Executor进程以多线程的方式运行任务，减少了多进程任务频繁的启动开销，使得任务执行变得非常高效和可靠。</li>
<li>Spark运行过程与资源管理器无关,只要能够获取Executor进程并保持通信就可以。</li>
<li>Executor上有一个<strong>BlockManager</strong>存储模块,类似于键盘存储系统。</li>
<li>任务采取了<strong>数据本地性</strong>和<strong>推测执行</strong>等优化机制。</li>

</ol>
<h3>三、应用场景实例</h3>
<blockquote><p>Spark是使用scala实现的基于内存计算的大数据开源集群计算环境，专门用于大数据量下的迭代式计算。</p>
</blockquote>
<ol>
<li><strong>复杂的批量数据处理</strong>：生态系统组件为Spark Core。</li>
<li><strong>基于历史数据的交互式查询</strong>：生态系统组件为Spark SQL。</li>
<li><strong>基于实时数据流的数据处理</strong>：生态系统组件为Spark Streaming。</li>
<li><strong>基于历史数据的数据挖掘</strong>：生态系统组件为MLlib。</li>
<li><strong>图结构数据的护理</strong>：生态系统组件为GraphX。</li>

</ol>
<h3>四、其他</h3>
<h3>核心API的拓展</h3>
<p>Spark Streaming是Spark核心API的一个扩展，可以实现<strong>高吞吐量</strong>的、具备<strong>容错机制</strong>的实时流数据的处理。</p>
<p><strong>学习建议</strong>：可以使用目前与大数据相关的内容，比如语雀、flomo、freeplane、listen1、幕布、flomo、MindMaster等,从中选择适合自己的软件或项目，同时做好整理和时间规划。</p>
